# Update system
sudo apt update && sudo apt upgrade -y

# Install Node.js 20.x (for Next.js 15)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Install Nginx
sudo apt install -y nginx

# Install PM2 (process manager)
sudo npm install -g pm2

# Install Git
sudo apt install -y git

# Create directory for your site
sudo mkdir -p /var/www/serve
sudo chown -R $USER:$USER /var/www/serve

cd /var/www/serve



# Clone your repository
git clone https://github.com/xerud2002/Serve.git .

# Create .env.local file with production values
cat > .env.local << 'EOF'
# Facebook Integration
FACEBOOK_ACCESS_TOKEN=EAAZADjQJDkC8BP6mf3L3K5tSCOrnPDeGONToIBWzIbF5f5oLpEuqqBWLnJyqFu30qKPuqdomaYnyISpYasV13iPNP09aYZAwApo41zujGZA5T5S8NFqHcffZBqwG8Dm0RjG6bliGIvZCjcAW0ddpabwKvsDOZBQVAJVt2xJLRpbncZBIXpH712BsEwMWixoZA2q50KBnsN1bSNTU6KnvCBbpxZCw9iNBJflabZAGbnRFgZD
FACEBOOK_PAGE_ID=239416516576684

# Resend Email
RESEND_API_KEY=re_UVrtTpQG_GmpHatleUGmFLbzYErSQNaja
EOF

# Install dependencies
npm install

# Build for production
npm run build


sudo nano /etc/nginx/sites-available/serve.org.uk

server {
    listen 80;
    listen [::]:80;
    server_name serve.org.uk www.serve.org.uk;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Next.js static files
    location /_next/static {
        proxy_cache STATIC;
        proxy_pass http://localhost:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Next.js images
    location /_next/image {
        proxy_pass http://localhost:3000;
        add_header Cache-Control "public, max-age=31536000";
    }

    # Public static files
    location /images {
        proxy_pass http://localhost:3000;
        add_header Cache-Control "public, max-age=86400";
    }
}




# Enable site
sudo ln -s /etc/nginx/sites-available/serve.org.uk /etc/nginx/sites-enabled/

# Remove default site
sudo rm /etc/nginx/sites-enabled/default

# Test configuration
sudo nginx -t

# Restart Nginx
sudo systemctl restart nginx


cd /var/www/serve

# Start with PM2
pm2 start npm --name "serve-website" -- start

# Save PM2 process list
pm2 save

# Enable PM2 on boot
pm2 startup
# Copy and run the command it outputs



# Install Certbot
sudo apt install -y certbot python3-certbot-nginx

# Get SSL certificate
sudo certbot --nginx -d serve.org.uk -d www.serve.org.uk

# Auto-renewal is configured automatically


# SSH into VPS
ssh webadmin@92.205.108.255

# Navigate to project
cd /var/www/serve

# Pull latest changes
git pull

# Install any new dependencies
npm install

# Rebuild
npm run build

# Restart the application
pm2 restart serve-website



ESSENTIALS

# Check PM2 status
pm2 status

# View application logs
pm2 logs serve-website

# Restart application
pm2 restart serve-website

# Check Nginx status
sudo systemctl status nginx

# View Nginx logs
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Update code from GitHub
cd /var/www/serve
git pull
npm install
npm run build
pm2 restart serve-website



ssh webadmin@92.205.108.255 "cd /var/www/serve && git pull && npm run build && pm2 restart serve-website"











